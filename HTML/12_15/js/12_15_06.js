function addNum(...numbers) {
  let sum = 0;

  for(let number of numbers)
    sum += number; // sum = sum + number
    
    return
}

console.log(addNum(1,3));
console.log(addNum(1,3,5,7));
// 전개 구문이란 값을 펼쳐주는 구문입니다
// 전개 구문에서 ... 기호를 사용합니다
// 배열에 있는 값만 꺼내 펼쳐서 전개구문으로 보여니다의 의미입니다